{
  "name": "cs-554-lab-6",
  "version": "1.0.0",
  "description": "A monolithic package which contains the frontend AND backend components of my Lab 6 submission.",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client && npm install && cd ..",
    "start-server": "cd server && npm start",
    "start-client": "cd client && npm start",
    "start": "concurrently \"npm run start-server\" \"npm run start-client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peter Rauscher (10441546)",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
